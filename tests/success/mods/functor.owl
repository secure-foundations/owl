locality alice

module S = { 
    name x : nonce @ alice
}

module Srefined = {
    name x : nonce @ alice
    name z : nonce @ alice
}

module T = { }

module blah = {
    name bar : nonce @ alice
}
                 
module M (X : S) = { 
    module Z : T = X
}

module N1 = M(Srefined)
