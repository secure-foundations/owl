locality alice

name d : nonce @ alice
name k : kdf {ikm info.
    True -> strict kdf {ikm2 info2.
        True -> enckey Name(d)
    }
} @ alice

def main() @ alice : Unit = 
    corr_case k in
    let k2 = kdf<0;;kdfkey;0>(get(k), 0x, 0x) in 
    let ek = kdf<0;;enckey;0>(k2, 0x, 0x) in 
    let c = aenc(ek, get(d)) in 
    output c



