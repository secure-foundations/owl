locality alice
name n<i> : nonce @ alice
name m<i> : nonce @ alice
name k : nonce @ alice

random_oracle l<i> : get(n<i>) -> nonce
random_oracle l2<i> : get(m<i>) -> nonce
random_oracle l3 : get(k) -> nonce

def alice<i>() @ alice : Unit = 
    corr_case n<i> in 
    let x = hash<l, i>(get(n<i>)) in 
    ()

