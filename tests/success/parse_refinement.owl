locality alice

name n : nonce @ alice
name m : nonce @ alice

struct T {
    x : Data<adv> | |nonce| |,
    y : Data<adv> | |nonce| |
}

def main(i : T) @ alice : Unit = 
    assume (x(i) == get(n));
    parse i as T(x, y) in 
    assert (x == x(i));
    assert (x == get(n));
    ()
