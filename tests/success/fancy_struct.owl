locality alice
name X : nonce @ alice
name P : nonce @ alice
name Q : nonce @ alice
name R : nonce @ alice

enum z_enum {
    | P Name(P)
    | Q Name(Q)
    | R 
}

struct s {
    _r : Name(R),
    _p : Name(P)
}

struct t {
    _x : Name(X),
    _y : Data<adv> ||nonce||,
    // _z : z_enum,
    _s : s
}

def main(x : t) @ alice : Unit = 
    assert True
