locality alice
name X : nonce @ alice
name P : nonce @ alice
name Q : nonce @ alice
name R : nonce @ alice

func some_length
    arity 0

enum z_enum {
    | P Name(P)
    | Q Name(Q)
}

struct s {
    _r : Name(R)
}

struct t {
    _x : Name(X),
    _y : Data<adv> |some_length()|,
    _z : z_enum,
    _s : s
}

def main(x : t) @ alice : Unit = 
    assert True
