locality alice

name n : nonce @ alice

kdf_group G {
    name k : kdfkey @ alice

    // Rule declares an enckey output
    kdf L : k, 0x, 0x01 -> enckey Name(n)
}

def alice_main() @ alice : Unit =
    // SHOULD FAIL: rule L declares enckey output, but name kinds says kdfkey
    let x = kdf<L;kdfkey;0>(get(k), 0x, 0x01) in
    ()
