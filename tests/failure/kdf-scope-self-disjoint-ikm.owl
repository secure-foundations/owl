locality alice

name n : nonce @ alice

kdf_scope G {
    name k : kdfkey @ alice

    // SHOULD FAIL: a ++ b in ikm is untagged concat, so
    // (a=0x12, b=0x34) and (a=0x1234, b=0x) produce the same ikm = 0x1234
    kdf L(a, b) : k, a ++ b, 0x -> enckey Name(n)
}
